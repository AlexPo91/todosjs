const initialState = {
  todos: [],
}

const todoReducer = (state = initialState, action) => {
  switch (action) {
    case action:
      return state
    default:
      return state
  }
}
export default todoReducer
